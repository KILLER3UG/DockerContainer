version: '3.8'

services:
  ai-gateway:
    build: .
    container_name: kilo-ai-gateway
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./kilo.json:/app/kilo.json:ro
      - gateway_cache:/app/.cache

volumes:
  gateway_cache:
